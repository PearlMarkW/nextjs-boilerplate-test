import Layout from './app/layout';

const Home = () => {
  return (
    <Layout>
      <section>
        <h1>Welcome to WiseStrats</h1>
        <p>Your partner for data-driven private equity due diligence.</p>
        {/* Add more sections like service overviews, client testimonials, etc. */}
      </section>
    </Layout>
  );
};

export default Home;
